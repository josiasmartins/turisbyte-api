// src/controllers/userController.ts
import { Request, Response } from 'express';

export const getUserProfile = (req: Request, res: Response) => {
  // Implemente a lógica para retornar o perfil do usuário autenticado aqui
};
